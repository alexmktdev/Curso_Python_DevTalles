class Persona:
    # constructor de la clase 
    # el nombre y edad de la izquierda, son propios del objeto 
    def __init__(self, nombre, edad): # parametros de la clase - self -> objeto propio a la clase
        self.nombre = nombre # el nombre de la derecha, viene de afuera, como argumento 
        self.edad = edad 

        '''
        self.nombre = nombre
        self.edad = edad 

        mi nombre = que nombre lleva ? el que le pasen como argumento - nombre 
        mi edad =  que edad lleva? el que le pasen como argumento - edad 

        '''


